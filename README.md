# 🔭 Omniscope

TUI-менеджер библиотеки для книг и научных статей. Работает в терминале, управляется с клавиатуры как Vim, умеет работать с DOI/arXiv/BibTeX и обладает встроенным AI-агентом.

Grounded in простой идее: хорошая библиотечная программа не должна требовать Electron.

[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)
[![Built with Rust](https://img.shields.io/badge/Built%20with-Rust-orange.svg)](https://www.rust-lang.org/)
[![Status: Active Development](https://img.shields.io/badge/Status-Active%20Development-yellow.svg)]()

---

## Что это

Трёхпанельный TUI (Terminal User Interface) с Vim-управлением и встроенным AI-агентом, который знает вашу коллекцию. Это высокопроизводительная альтернатива Zotero для тех, кто ценит скорость, клавиатурное управление и минимализм.

---

## Текущий статус (v0.1.0-alpha)

Проект находится в стадии активной разработки. Текущая версия обозначена как **alpha 0.1**. **Фаза 1 (Ядро TUI)** и **Фаза 2 (Поиск и Vim-движок)** практически завершены.

### ✅ Что уже работает:

<<<<<<< gemini
**The terminal is enough.** Three-panel layout. Vim grammar (`[count][verb][noun]`). Seven modes. Telescope-style search with a full DSL. Everything a GUI library manager does, but from the terminal, at the speed of keystrokes.

**The AI is a librarian, not a chatbot.** Omniscope's AI layer — named the Librarian — knows your entire collection through a compact Library Map. It extracts references from papers, suggests reading order, finds knowledge gaps, and acts through a structured Action Protocol that logs every change and supports undo. It works with Claude, GPT, Gemini, or a local Ollama model.
=======
*   **Vim-движок "из коробки"**: Полная грамматика `[count][operator][motion/text-object]`.
    *   **Режимы**: Normal, Insert, Visual (Char, Line, Block), Command, Search, Pending.
    *   **Навигация и Скроллинг**: `j/k`, `gg/G`, `Ctrl+d/u/f/b`, `Ctrl+e/y` (без сдвига курсора), `zz/zt/zb` (viewport offset), `H/M/L`, иерархические прыжки (`-`, `gp`, `gr`, `gF`).
    *   **Операторы**: `d` (удаление), `y` (yank), `c` (быстрое изменение `cs/cr/cy/ct`), `>` (добавить тег), `<` (удалить тег).
    *   **Текстовые объекты**: Полная семантика `inner` и `around` для сущностей: `ib/ab` (книга), `il/al` (библиотека), `it/at` (тег), `if/af` (папка/фильтр).
    *   **Регистры, Метки и Макросы**: Запись и воспроизведение макросов (`q`, `@`, `@@`); 10 цифровых и 26 буквенных регистров; буфер обмена (`"+`); локальные метки и автометки (`''`, `'<`/`'>`).
    *   **Внутристраничный поиск**: Прямой (`/`) и обратный (`?`), повтор (`n/N`), поиск автора под курсором (`*/#`).
    *   **Командный режим**: Поддержка автодополнения (Tab/Shift+Tab) с отображением списка кандидатов над командной строкой.
    *   **Улучшенный UI**: Контекстные подсказки (hints) над статус-баром, визуализация пользовательских меток (marks) в списке файлов (например, `'a`, `✓`).
    *   **Undo/Redo**: Бесконечный стек отмен с корректной работой в визуальных режимах и поддержкой временных прыжков (`:earlier 5m`).
*   **Умный Поиск и Фильтрация**:
    *   **Fuzzy-поиск**: Мгновенный поиск через `nucleo` с поддержкой Telescope-подобного интерфейса.
    *   **EasyMotion**: Прыжки по экрану через `Space Space`.
    *   **f/F/t/T**: Быстрая навигация по символам в заголовках.
    *   **DSL-парсер**: Сложные запросы типа `@author:knuth #algorithms year:>2020`.
*   **Производительность**:
    *   Холодный старт **< 50ms**.
    *   Виртуализированные списки для работы с тысячами книг без лагов.
    *   Frecency-ранжирование (алгоритм на основе частоты и новизны использования).
*   **Управление данными и хранением (Folder System)**:
    *   **Дерево папок (Sidebar)**: Полноценное управление структурой через левую панель (`Tab` для переключения). Поддержка Vim-навигации, `za/zo` фолдинга и массовых операций (Visual mode).
    *   **Folder View (Center)**: Режим просмотра содержимого папок с поддержкой хлебных крошек и быстрой навигации (`-` для возврата).
    *   **Sync Panel (`:sync`)**: Специализированный интерфейс для синхронизации БД и диска. Позволяет находить новые файлы и восстанавливать "оторванные" записи.
    *   **Ghost Books**: Поддержка книг без физических файлов. Клавиша `gf` проверяет наличие файла, `E` — позволяет прикрепить существующий файл с автодополнением пути (`Tab`).
    *   **Умное сканирование**: Автоматическое обнаружение новых файлов через `notify` (библиотека `watcher`) с защитой от коллизий имён и массовым созданием карточек.
    *   **Децентрализованная архитектура `.libr/`**: Метаданные хранятся вместе с книгами (аналог `.git`). Поддержка неограниченного числа независимых библиотек.
    *   Трёхпанельный интерфейс браузинга (Библиотеки — Список — Предпросмотр).
    *   SQLite + FTS5 для мгновенного полнотекстового поиска.
    *   Quickfix-списки (`Ctrl+q`) для пакетных операций.
>>>>>>> local

---

## Технологический стек

| Компонент | Технология |
|---|---|
| **Ядро** | Rust (Edition 2024), Workspace из 7 крейтов |
| **Интерфейс** | Ratatui, Crossterm |
| **База данных** | SQLite (rusqlite) + FTS5 |
| **Поиск** | Nucleo (Fuzzy), Tantivy (в планах), Custom DSL Parser |
| **Асинхронность** | Tokio |
| **AI** | Ollama, Anthropic API (в разработке) |

---

## Дорожная карта

*   **Фаза 3: Научный модуль**: Автоматическое извлечение метаданных (DOI, arXiv, ISBN), интеграция с BibTeX.
*   **Фаза 4: AI-агент**: Семантический поиск, автоматическое тегирование, генерация саммари через локальные LLM.
*   **Фаза 5: Синхронизация**: Собственный сервер на Axum для синхронизации библиотеки между устройствами.

---

## Документация

*   [`OMNISCOPE_MASTER_PLAN.md`](OMNISCOPE_MASTER_PLAN.md) — детальная дорожная карта.
*   [`Omniscope_VIM_MOTIONS.md`](Omniscope_VIM_MOTIONS.md) — спецификация Vim-языка.
*   [`VIM_MOTIONS_IMPL_PLAN.md`](VIM_MOTIONS_IMPL_PLAN.md) — пошаговый план реализации (почти выполнен).
*   [`Omniscope_AI_SYSTEM.md`](Omniscope_AI_SYSTEM.md) — архитектура AI-модуля.
*   [`docs/Library_Management_and_Storage.md`](docs/Library_Management_and_Storage.md) — архитектура хранения данных (`.libr/`), управление библиотеками и папками.
