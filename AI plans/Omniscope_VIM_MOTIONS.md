# 📚 omniscope — Vim Motions: Полная спецификация

> Этот документ описывает полную систему управления omniscope через клавиатуру.
> Философия: vim motions — не просто горячие клавиши, это **язык** для работы с библиотекой.
> Каждая операция выражается как глагол + объект + модификатор.

---

## 0. Философия: Vim как язык

В vim каждая команда строится по грамматике:

```
[count] [operator] [motion/text-object]
```

В omniscope эта грамматика расширяется на управление библиотекой:

```
[count] [verb] [noun]
```

| Компонент | Примеры |
|---|---|
| **Count** | `3`, `5`, `10` — применить N раз |
| **Verb (оператор)** | `d` delete, `y` yank, `m` move, `c` change, `p` put |
| **Noun (объект)** | `b` book, `t` tag, `l` library, `f` folder, `s` selection |

Примеры:
```
3j          → перейти на 3 книги вниз
5dd         → удалить 5 книг
2mb         → переместить 2 книги в буфер
ytl         → yank (скопировать) тег в библиотеку
```

---

## 1. Режимы (Modes)

omniscope имеет **7 режимов**, каждый со своим визуальным индикатором в статус-баре:

```
┌────────────────────────────────────────────────────────────────┐
│ NORMAL │ INSERT │ VISUAL │ VISUAL-LINE │ COMMAND │ SEARCH │ PENDING │
└────────────────────────────────────────────────────────────────┘
```

| Режим | Индикатор | Назначение |
|---|---|---|
| **NORMAL** | `[N]` синий | Навигация, операторы |
| **INSERT** | `[I]` зелёный | Редактирование полей карточки |
| **VISUAL** | `[V]` оранжевый | Выделение отдельных книг |
| **VISUAL-LINE** | `[VL]` оранжевый | Выделение диапазона строк |
| **VISUAL-BLOCK** | `[VB]` оранжевый | Блочное выделение (колонки) |
| **COMMAND** | `[:]` белый | Командная строка |
| **SEARCH** | `[/]` жёлтый | Telescope поиск |
| **PENDING** | `[g?]` серый | Ожидание второй клавиши оператора |

### Переходы между режимами

```
         Esc / Ctrl+c
    ┌──────────────────────┐
    ↓                      │
 NORMAL ─────i/a/o──────► INSERT
    │                      │
    ├─────v──────────────► VISUAL
    │                      │
    ├─────V──────────────► VISUAL-LINE
    │                      │
    ├─────Ctrl+v─────────► VISUAL-BLOCK
    │                      │
    ├─────:──────────────► COMMAND
    │                      │
    └─────/ or ?─────────► SEARCH (telescope)
```

---

## 2. NORMAL Mode — Полная карта

### 2.1 Базовая навигация

```
── ВЕРТИКАЛЬНАЯ НАВИГАЦИЯ ─────────────────────────────────────

j               Следующая книга (↓)
k               Предыдущая книга (↑)
[count]j        Перейти на N книг вниз          5j = 5 книг вниз
[count]k        Перейти на N книг вверх          3k = 3 книги вверх

Ctrl+d          Прокрутить на пол-экрана вниз
Ctrl+u          Прокрутить на пол-экрана вверх
Ctrl+f          Прокрутить на полный экран вниз
Ctrl+b          Прокрутить на полный экран вверх
Ctrl+e          Прокрутить на 1 строку вниз (без движения курсора)
Ctrl+y          Прокрутить на 1 строку вверх (без движения курсора)

gg              Перейти к первой книге (top)
G               Перейти к последней книге (bottom)
[count]G        Перейти к N-й книге по индексу   10G = 10-я книга
[count]gg       То же что [count]G

H               Перейти к верхней видимой книге (High)
M               Перейти к средней видимой книге (Middle)
L               Перейти к нижней видимой книге (Low)

── ГОРИЗОНТАЛЬНАЯ НАВИГАЦИЯ (между панелями) ──────────────────

h               Перейти в левую панель
l               Перейти в правую панель
Ctrl+h          Перейти в левую панель (всегда, из любого места)
Ctrl+l          Перейти в правую панель (всегда)
Tab             Следующая панель (циклически)
Shift+Tab       Предыдущая панель (циклически)

── ПРЫЖКИ ПО ГРУППАМ ──────────────────────────────────────────

{               Предыдущая группа (папка/библиотека/тег-секция)
}               Следующая группа
[[              Предыдущая библиотека
]]              Следующая библиотека
[t              Предыдущий тег в списке тегов (левая панель)
]t              Следующий тег в списке тегов

── НАВИГАЦИЯ ПО ИЕРАРХИИ ──────────────────────────────────────

-               Выйти на уровень выше (папка → библиотека → All)
                Аналог "-" в vim (открыть netrw родителя)
Enter           Войти в папку / открыть книгу
Backspace       Синоним "-"

gp              Перейти к родительскому элементу (parent)
gr              Перейти к корню библиотеки (root)
gh              Перейти в "All Books" (home)

── JUMP LIST (история переходов) ──────────────────────────────

Ctrl+o          Назад по истории переходов (jump back)
Ctrl+i          Вперёд по истории переходов (jump forward)
''              Перейти к предыдущей позиции (как '' в vim)
```

### 2.2 Операторы — глаголы языка

Операторы ожидают motion или text-object после нажатия:

```
── ОПЕРАТОРЫ ──────────────────────────────────────────────────

d               [Delete operator] — ожидает motion
                  dd     удалить текущую книгу
                  d3j    удалить 3 книги вниз
                  dG     удалить от курсора до конца
                  dgg    удалить от начала до курсора
                  dil    удалить всё внутри текущей библиотеки
                  dat    удалить всё с текущим тегом
                  diw    удалить книгу под курсором

y               [Yank operator] — копировать в буфер обмена
                  yy     yanked текущую книгу (путь к файлу)
                  Y      yank полную JSON-карточку
                  y3j    yank 3 книги (их ID/пути)
                  yil    yank все книги в текущей библиотеке

c               [Change operator] — изменить поле
                  cc     открыть форму редактирования текущей книги
                  ct     change tags (редактировать теги)
                  cs     change status (изменить статус чтения)
                  cr     change rating (изменить рейтинг)
                  ca     change author
                  cT     change title
                  cy     change year

m               [Move operator] — переместить
                  ml     move to library (выбрать библиотеку)
                  mf     move to folder (выбрать папку)
                  mt     move tag (перетегировать)
                  m3j    переместить 3 книги вниз в выборку

p               [Put operator] — вставить из буфера
                  p      вставить yanked книги в текущую библиотеку
                  P      вставить перед текущей позицией

>               Добавить тег к книге(ам)
<               Убрать тег с книги(ам)
=               Выровнять/нормализовать метаданные (через AI)
```

### 2.3 Text Objects — существительные языка

Text objects работают с операторами: `[operator][i/a][object]`

`i` = "inner" (только содержимое), `a` = "around" (с контейнером)

```
── TEXT OBJECTS ───────────────────────────────────────────────

Объекты:
  b   book         текущая книга
  l   library      текущая библиотека
  t   tag          книги с текущим тегом
  f   folder       текущая папка
  s   selection    текущее выделение
  a   author       все книги этого автора
  y   year         все книги этого года

Примеры:
  dil     delete inner library  → удалить все книги в библиотеке
  dal     delete around library → удалить библиотеку и все книги
  yil     yank inner library    → скопировать все книги библиотеки
  dit     delete inner tag      → удалить тег со всех книг
  cit     change inner tag      → переименовать тег (во всей библиотеке)
  mil     move inner library    → переместить все книги библиотеки
  dia     delete inner author   → удалить все книги этого автора
  yit     yank inner tag        → скопировать список книг с тегом
  vit     visual select tag     → выделить все книги с тегом
  vil     visual select library → выделить все книги библиотеки
```

### 2.4 g-команды (g-prefix)

```
── g КОМАНДЫ ──────────────────────────────────────────────────

Навигация:
  gh          goto home (All Books)
  gl          goto last (последняя посещённая позиция)
  gb          goto buffer list (список буферов)
  gB          goto previous buffer
  gf          goto file (открыть файл книги в системе)
  gF          goto file folder (открыть папку файла в файловом менеджере)
  gp          goto parent (родительская папка/библиотека)
  gr          goto root (корень библиотеки)
  g/          goto search (telescope с историей)
  g?          показать справку по текущей панели

Информация:
  gi          goto info (открыть карточку в preview панели)
  gI          goto Info full (открыть карточку в $EDITOR)
  gd          goto definition = goto source file
  gD          goto file directory
  gn          goto next (следующий результат поиска)
  gN          goto previous (предыдущий результат поиска)

Действия:
  ga          goto add = добавить книгу (wizard)
  gA          goto Add from file
  gc          goto create = создать папку/библиотеку/тег (submenu)
  gC          goto Calibre import
  gZ          goto Zotero import
  gs          goto sort (меню сортировки)
  gS          goto Stats (статистика библиотеки)
  gm          goto marks (список marks)
  g`{mark}    goto mark {mark}
  gq          format/normalize карточку через AI

Отображение:
  gv          toggle preview (показать/скрыть правую панель)
  gV          toggle verbose preview (расширенная информация)
  gw          toggle wrap в preview
  gz          toggle zoom (развернуть текущую панель)
```

### 2.5 z-команды (z-prefix) — вид и складки

```
── z КОМАНДЫ ──────────────────────────────────────────────────

Центрирование:
  zz          Центрировать текущую книгу на экране
  zt          Scroll так, чтобы текущая книга была вверху (top)
  zb          Scroll так, чтобы текущая книга была внизу (bottom)

Складки (группировки):
  za          toggle fold — свернуть/развернуть группу
  zo          open fold — развернуть папку/группу
  zc          close fold — свернуть папку/группу
  zR          open all folds — развернуть всю иерархию
  zM          close all folds — свернуть всю иерархию
  zj          перейти к следующей свёрнутой группе
  zk          перейти к предыдущей свёрнутой группе

Отображение:
  zh          scroll preview влево (горизонтальный скролл)
  zl          scroll preview вправо
  zH          scroll preview на пол-ширины влево
  zL          scroll preview на пол-ширины вправо
```

### 2.6 Marks (закладки)

Полная система marks как в vim:

```
── MARKS ──────────────────────────────────────────────────────

Установка:
  m{a-z}      Установить локальный mark (в текущей библиотеке)
              ma = mark 'a', mb = mark 'b' ... mz = mark 'z'

  m{A-Z}      Установить глобальный mark (работает между библиотеками)
              mA = глобальный mark 'A', mB = 'B' ...

Переходы:
  '{a-z}      Перейти к mark (к книге с этим mark)
  `{a-z}      Перейти к mark (точная позиция)
  '{A-Z}      Перейти к глобальному mark (другая библиотека!)
  ''          Прыжок назад (предыдущая позиция)
  '`          То же

Специальные marks (автоматические):
  '.          Последнее изменение
  '^          Последняя позиция INSERT mode
  '[          Начало последнего yank/change
  ']          Конец последнего yank/change
  '<          Начало последнего visual selection
  '>          Конец последнего visual selection

Управление:
  :marks      Показать все marks
  :marks {a-z}  Показать конкретные marks
  :delmarks {a-z}  Удалить marks
  :delmarks!  Удалить все локальные marks
```

### 2.7 Registers (буферы)

Именованные буферы для хранения книг и карточек:

```
── REGISTERS ──────────────────────────────────────────────────

Именованные регистры:
  "{a-z}[operator]    Использовать регистр {a-z}

  "ayy        Yank книгу в регистр 'a'
  "bdd        Delete книги в регистр 'b' (сохраняет перед удалением)
  "ap         Paste из регистра 'a'
  "Ayy        Append к регистру 'A' (ЗАГЛАВНАЯ = добавить, не заменить)

Специальные регистры:
  ""          Безымянный регистр (последний yank/delete)
  "0          Yank регистр (только последний yank, не delete)
  "1-"9       История удалений (1 = последнее, 9 = самое старое)
  "+          Системный буфер обмена
  "*          Первичный буфер (X11)
  "_          Black hole регистр (удалить без сохранения)
  ".          Последний INSERT режим текст
  "%          Имя/путь текущего файла книги
  ":          Последняя команда
  "/          Последний поисковый запрос

Просмотр регистров:
  :reg        Показать все регистры
  :reg {a}    Показать конкретный регистр

Примеры использования:
  "ayil       Yank все книги библиотеки в регистр 'a'
  "bvit"bd    Visual select по тегу → delete в регистр 'b'
  "+yy        Yank путь к файлу в системный буфер обмена
  "1p         Paste последнего удалённого
  "_dd        Удалить книгу БЕЗ сохранения в регистр
```

### 2.8 Числовые префиксы (counts)

```
── ЧИСЛОВЫЕ ПРЕФИКСЫ ──────────────────────────────────────────

5j          Вниз на 5 книг
10k         Вверх на 10 книг
3dd         Удалить 3 книги (текущую + 2 следующих)
2yy         Yank 2 книги
4p          Paste 4 раза
7G          Перейти к 7-й книге в списке
2Ctrl+d     Прокрутить вниз в 2 раза (2 * полэкрана)

# Числа + операторы:
3mit        Переместить 3 книги в целевую библиотеку (интерактивно)
2cc         Открыть форму для изменения 2 книг последовательно
5>          Добавить тег к 5 книгам вниз
```

---

## 3. INSERT Mode — Редактирование карточки

INSERT mode активируется при редактировании полей книги.
Это **форма с полями**, перемещение по полям аналогично vim.

### 3.1 Вход в INSERT mode

```
── ВХОД В INSERT MODE ─────────────────────────────────────────

i           Insert — редактировать текущее поле карточки
I           Insert at beginning — перейти к первому полю и начать
a           Append — редактировать поле, курсор в конец
A           Append at end — перейти к последнему полю
o           Open new note — создать новую заметку к книге
O           Open note above — создать заметку в начале списка

cc          Change current — очистить поле и войти в редактирование
C           Change to end — очистить от курсора до конца поля
s           Substitute — удалить символ под курсором и войти
S           Substitute line — очистить всё поле и войти

r           Replace one — заменить один символ (без INSERT mode)
R           Replace mode — замена символов (не INSERT, а REPLACE)
```

### 3.2 Навигация по форме карточки

```
Поле карточки (пример):
┌────────────────────────────────────────────────────────────┐
│ Title:    [The Rust Programming Language              ]   │
│ Authors:  [Steve Klabnik, Carol Nichols               ]   │  ← курсор
│ Year:     [2023                                       ]   │
│ ISBN:     [9781718503106                              ]   │
│ Tags:     [rust, systems, beginner                   ]   │
│ Library:  [programming                               ]   │
│ Status:   [read                                      ]   │
│ Rating:   [★★★★★                                     ]   │
│ Notes:    [                                          ]   │
└────────────────────────────────────────────────────────────┘
```

```
── НАВИГАЦИЯ ПО ПОЛЯМ (INSERT mode) ───────────────────────────

Tab         Следующее поле
Shift+Tab   Предыдущее поле
Enter       Подтвердить поле и перейти к следующему
Esc         Выйти из INSERT, вернуться в NORMAL

Ctrl+n      Next field (как Tab)
Ctrl+p      Previous field (как Shift+Tab)

── ВНУТРИ ПОЛЯ (INSERT mode) ──────────────────────────────────

Ctrl+w      Удалить слово назад (как в vim INSERT)
Ctrl+u      Удалить от курсора до начала поля
Ctrl+k      Удалить от курсора до конца поля
Ctrl+h      Backspace
Ctrl+a      Перейти в начало поля
Ctrl+e      Перейти в конец поля
Ctrl+b      Слово назад
Ctrl+f      Слово вперёд

── СПЕЦИАЛЬНЫЕ ПОЛЯ ───────────────────────────────────────────

Tags поле:
  ,         Разделитель тегов (автодополнение после каждого)
  Ctrl+x    Autocomplete тег из существующих
  Tab       Принять первое предложение автодополнения

Library поле:
  Ctrl+x    Fuzzy выбрать библиотеку
  :new      Создать новую библиотеку прямо здесь

Rating поле:
  1-5       Установить рейтинг цифрой
  +         Увеличить рейтинг
  -         Уменьшить рейтинг

Status поле:
  u         unread
  r         reading
  R         read (done)
  d         dnf (did not finish)

── СОХРАНЕНИЕ И ОТМЕНА ────────────────────────────────────────

Ctrl+s      Сохранить карточку (остаться в INSERT)
Esc         Выйти в NORMAL (сохраняет автоматически)
:w          Принудительное сохранение
:q!         Отменить все изменения и выйти
Ctrl+z      Отменить последнее изменение в поле
```

### 3.3 Быстрое изменение конкретных полей (из NORMAL)

Без полного открытия формы, мгновенное изменение одного поля:

```
── БЫСТРОЕ РЕДАКТИРОВАНИЕ (NORMAL mode) ───────────────────────

cT          change Title    → inline редактирование заголовка
ca          change author   → редактировать авторов
cy          change year     → редактировать год
ct          change tags     → редактировать теги (tag picker)
cl          change library  → выбрать библиотеку (fuzzy picker)
cs          change status   → выбрать статус (mini-menu)
cr          change rating   → ввести рейтинг 1-5
cn          change note     → добавить/редактировать заметку
ci          change isbn     → редактировать ISBN
cp          change path     → изменить путь к файлу

# Работают с count:
3ct         Изменить теги у 3 книг (последовательно)
vj cs       Visual select 2 книги → изменить статус обеих
```

---

## 4. VISUAL Mode — Выделение и групповые операции

### 4.1 Три вида Visual mode

```
v           VISUAL         — выбор отдельных книг (toggle per item)
V           VISUAL-LINE    — выбор диапазона (как строки в vim)
Ctrl+v      VISUAL-BLOCK   — блочный выбор (для операций с полями)
```

### 4.2 VISUAL mode — выбор книг

```
── VISUAL MODE ────────────────────────────────────────────────

Вход:
  v           Toggle выделение текущей книги, перейти вниз
  V           Начать выделение диапазона

В VISUAL mode:
  j / k       Расширить выделение вниз/вверх
  gg / G      Расширить до начала/конца
  H / M / L   Расширить до верхней/средней/нижней видимой
  it          Select all books with same tag (text object)
  il          Select all books in current library
  ia          Select all books by same author

Операторы в VISUAL mode:
  d           Удалить все выделенные книги
  y           Yank (скопировать) выделенные книги
  m           Переместить выделенные → выбрать цель
  c           Изменить поле у всех выделенных
  cs          Изменить статус у всех
  cr          Изменить рейтинг у всех
  ct          Изменить теги у всех (добавить/заменить)
  >           Добавить тег ко всем выделенным
  <           Убрать тег со всех выделенных
  @i          AI-индексировать все выделенные
  @s          AI: создать summaries для всех
  o           Open all (открыть все файлы)
  :           Командная строка с контекстом выделения

Специальные выделения:
  gv          Восстановить предыдущее выделение (как gv в vim)
  o           В VISUAL mode: перейти к другому концу выделения

Выход:
  Esc         Снять выделение, вернуться в NORMAL
  v           Если уже в VISUAL — снять выделение
```

### 4.3 VISUAL-BLOCK — работа с полями нескольких книг

Уникальная возможность: редактировать одно поле сразу у нескольких книг:

```
Ctrl+v      Начать блочный выбор

Пример:
  Ctrl+v      Начать
  4j          Выделить 5 книг вертикально
  $           Расширить до конца строки (или 'cr' для поля Rating)
  I           Вставить одинаковый текст ко всем
  Esc         Применить ко всем

Реальный кейс:
  # Установить рейтинг 5 для 5 книг:
  Ctrl+v → 4j → cr → 5 → Esc

  # Добавить тег "archived" к 10 книгам:
  Ctrl+v → 9j → > → archived → Enter
```

---

## 5. Операции с файлами и организацией — детально

### 5.1 Создание объектов

```
── СОЗДАНИЕ (gc = go create) ──────────────────────────────────

gcl         go create library    → создать новую библиотеку
gcf         go create folder     → создать папку в текущей библиотеке
gct         go create tag        → создать новый тег
gcb         go create book       → создать карточку книги (без файла)
gcB         go create Book (wizard) → мастер добавления с файлом
gcn         go create note       → заметка к текущей книге

# Альтернативы:
a           add book (wizard)    — быстрый вариант gcB
A           Add book from file   — выбрать файл → создать карточку
:new lib    создать библиотеку из командной строки
:new folder создать папку
:new tag    создать тег

── ДЕТАЛИ СОЗДАНИЯ ПАПКИ ──────────────────────────────────────

gcf     →   Prompt: "Folder name: " с подсказкой текущего пути
            Поддерживает вложенность через /: "rust/advanced/async"
            Автосоздание промежуточных папок

:mkdir rust/advanced/async      — создать вложенную иерархию
:mkdir -p rust/advanced/async   — создать с родителями (то же)
```

### 5.2 Переименование и изменение

```
── ПЕРЕИМЕНОВАНИЕ ─────────────────────────────────────────────

r           Rename — переименовать книгу (изменить title)
            В папке: переименовать папку
            В библиотеке (курсор на название): переименовать библиотеку
            В теге (курсор на тег): переименовать тег

# Пример контекстного r:
  [курсор на книге]      r → переименовать книгу (edit title)
  [курсор на папке]      r → переименовать папку
  [курсор на теге]       r → переименовать тег (во всей библиотеке!)
  [курсор на библиотеке] r → переименовать библиотеку

R           Rename (полная форма) — переименовать с подтверждением
            Показывает старое имя, позволяет редактировать

cT          change Title — только для книги (как псевдоним)
```

### 5.3 Перемещение книг между библиотеками и папками

```
── ПЕРЕМЕЩЕНИЕ (m = move operator) ───────────────────────────

ml          move to Library     → fuzzy picker → выбрать библиотеку
mf          move to Folder      → fuzzy picker → выбрать папку
mt          move to Tag         → добавить тег (move + tag)
m<lib>      прямой move в библиотеку (при известном имени)

Примеры:
  ml                  Fuzzy picker всех библиотек → переместить
  mf                  Fuzzy picker всех папок → переместить
  3ml                 Переместить 3 книги (с курсора вниз)
  vitmL fiction       Visual select by tag → переместить все в 'fiction'

Перемещение через drag-and-drop (эмуляция):
  Space               Выбрать/отметить книгу для перемещения
  [navigate to target]
  p                   Paste = переместить отмеченную книгу сюда

# Буфер перемещения:
  yy                  Yank книгу (копирование)
  dd                  Cut книгу (перемещение)
  [перейти в цель]
  p                   Paste (поместить)

── CUT/COPY/PASTE ФЛОУ ────────────────────────────────────────

dd → [navigate] → p       Переместить книгу (cut + paste)
yy → [navigate] → p       Скопировать книгу в другую библиотеку
"add → [navigate] → "ap   То же через именованный регистр 'a'

# Пример: переместить все книги с тегом #old в архивную библиотеку
vit → выбрать все с тегом 'old' → dd → [перейти в archive] → p
```

### 5.4 Удаление с вариантами

```
── УДАЛЕНИЕ ───────────────────────────────────────────────────

dd          Delete card — удалить карточку (файл ОСТАЁТСЯ)
dD          Delete everything — удалить карточку + файл
d_          Delete file only — удалить файл (карточка остаётся)

:del        Удалить с подтверждением
:del!       Удалить без подтверждения
:del --with-file    Удалить всё

# С регистром (сохранить перед удалением):
"add        Удалить в регистр 'a' (можно восстановить через "ap)
"_dd        Удалить в black hole (без возможности восстановить)

# Групповое удаление:
dil         delete inner library → удалить все книги библиотеки
dat         delete around tag → удалить тег И все книги с ним
```

### 5.5 Undo / Redo — полная история изменений

```
── UNDO/REDO ──────────────────────────────────────────────────

u           Undo — отменить последнее действие
Ctrl+r      Redo — повторить отменённое
U           Undo all changes to current book (reset карточку)
            (осторожно: отменяет всё с момента открытия)

# История undo:
:undolist   Показать дерево истории изменений
:earlier 5  Откатиться на 5 изменений назад
:later 3    Перейти на 3 изменения вперёд
:earlier 1h Откатиться к состоянию час назад
:later 2m   Перейти к состоянию 2 минуты вперёд

# Постоянная история (сохраняется между сессиями):
# Undo tree сохраняется в ~/.local/share/omniscope/undo/
```

---

## 6. Поиск и Telescope — SEARCH mode

### 6.1 Вход в поиск

```
/           Открыть telescope (прямой поиск)
?           Открыть telescope (обратный — fuzzy по всем полям)
*           Поиск под курсором — найти книги с тем же тегом/автором
#           Поиск под курсором — назад

g/          Telescope с историей поисков
n           Следующий результат поиска
N           Предыдущий результат поиска
```

### 6.2 Внутри Telescope (SEARCH mode)

```
── ВНУТРИ TELESCOPE ───────────────────────────────────────────

Ввод:
  [typing]    Фильтрация результатов в реальном времени
  Ctrl+n      Следующий результат
  Ctrl+p      Предыдущий результат
  Ctrl+j      Следующий результат (альтернатива)
  Ctrl+k      Предыдущий результат

Действия с выбранным:
  Enter       Открыть книгу / перейти
  Tab         Отметить книгу (multiple select)
  Shift+Tab   Снять отметку
  Ctrl+q      Отправить все результаты в quickfix list
  Ctrl+l      Отправить отмеченные в quickfix list

Режимы поиска (переключение внутри telescope):
  Ctrl+f      Переключить: fuzzy / full-text / semantic
  Ctrl+r      Переключить: regex mode
  Alt+t       Фильтр: только по тегам
  Alt+a       Фильтр: только по авторам
  Alt+y       Фильтр: только по году

Действия без выхода:
  Ctrl+x      Открыть в split (preview справа)
  Ctrl+s      Изменить статус выбранной
  Ctrl+d      Удалить выбранную

  Esc         Закрыть telescope
  Ctrl+c      Закрыть telescope
```

### 6.3 Quickfix List — список результатов

Аналог quickfix в vim — список книг для последовательной обработки:

```
── QUICKFIX LIST ──────────────────────────────────────────────

:copen      Открыть quickfix список
:cclose     Закрыть quickfix список
:cnext / cn Следующий элемент (следующая книга)
:cprev / cp Предыдущий элемент
:cfirst     Первый элемент
:clast      Последний элемент

Ctrl+q      (в telescope) → отправить все результаты в quickfix

Кейс: найти все книги #rust → обработать каждую последовательно:
  /  → #rust → Ctrl+q   Отправить все #rust книги в quickfix
  :copen                 Открыть список
  cn                     Перейти к следующей и обработать

Location List (отдельный quickfix для каждого окна):
:lopen / :lclose
:lnext / :lprev
```

### 6.4 Команды поиска через командную строку

```
# Глобальные команды (аналог :g в vim)
:g/#rust/cs read       Изменить статус ВСЕХ книг с тегом #rust на "read"
:g/year:2020/cs unread Изменить статус всех книг 2020 года на unread
:g/author:Klabnik/ml fiction  Переместить все книги Klabnik в 'fiction'
:g/r:5/yy              Yank все книги с рейтингом 5 в регистр ""

# Инвертированный глобальный (аналог :v)
:v/has:file/dd         Удалить все карточки БЕЗ файла
:v/#tag/ai suggest-tags  AI предложит теги для всех непомеченных
```

---

## 7. Window / Panel Management

### 7.1 Панели как окна vim

```
── УПРАВЛЕНИЕ ПАНЕЛЯМИ ────────────────────────────────────────

Навигация:
  Ctrl+w h    Перейти в левую панель
  Ctrl+w l    Перейти в правую панель
  Ctrl+w j    Перейти в нижнюю панель (если есть)
  Ctrl+w k    Перейти в верхнюю панель
  Ctrl+w w    Следующая панель (циклически)
  Ctrl+w p    Предыдущая панель

Размеры:
  Ctrl+w =    Сравнять все панели
  Ctrl+w >    Увеличить ширину правой панели
  Ctrl+w <    Уменьшить ширину правой панели
  Ctrl+w +    Увеличить высоту панели
  Ctrl+w -    Уменьшить высоту панели
  Ctrl+w |    Максимизировать ширину текущей панели
  Ctrl+w _    Максимизировать высоту
  [N]Ctrl+w > Изменить ширину на N колонок

Управление панелями:
  Ctrl+w o    Only — скрыть все кроме текущей (zoom)
  gz          Zoom текущей панели (toggle, проще чем Ctrl+w o)
  Ctrl+w v    Открыть vertical split с preview
  Ctrl+w q    Закрыть панель
  Ctrl+w r    Rotate — сменить расположение панелей
```

### 7.2 Буферы — несколько библиотечных видов

Аналог vim buffers: каждый буфер — отдельный вид библиотеки:

```
── БУФЕРЫ ─────────────────────────────────────────────────────

:bnext / :bn    Следующий буфер
:bprev / :bp    Предыдущий буфер
:b {name}       Перейти к буферу по имени
:b {N}          Перейти к буферу N
:bd             Закрыть буфер
:bd!            Закрыть буфер принудительно
:buffers / :ls  Список всех буферов
:ball           Открыть все буферы в панелях

gb              goto buffer (telescope буферов)
gB              goto previous Buffer

# Типы буферов:
  [library]     Вид конкретной библиотеки
  [search]      Результаты поиска как буфер
  [tag:rust]    Все книги с тегом (буфер тега)
  [author:X]    Все книги автора
  [year:2023]   Все книги года
  [AI chat]     Буфер AI-разговора
  [card:uuid]   Открытая карточка книги

# Создать новый буфер:
:new            Новый пустой буфер (для заметок)
:new lib:prog   Буфер для библиотеки 'programming'
:new tag:rust   Буфер для тега 'rust'
:new search     Буфер поиска
```

### 7.3 Tabpages — вкладки для разных рабочих контекстов

```
── ВКЛАДКИ ────────────────────────────────────────────────────

Управление:
  :tabnew         Новая вкладка
  :tabnew lib:X   Новая вкладка с библиотекой X
  :tabclose       Закрыть вкладку
  :tabonly        Оставить только текущую вкладку
  :tabs           Список вкладок

Навигация:
  gt              Next tab (следующая вкладка)
  gT              Previous tab
  {N}gt           Перейти к N-й вкладке
  Ctrl+PageDown   Следующая вкладка
  Ctrl+PageUp     Предыдущая вкладка

Примеры использования вкладок:
  :tabnew lib:fiction    Открыть художественную литературу в новой вкладке
  :tabnew tag:rust       Открыть все rust-книги в новой вкладке
  :tabnew search         Отдельная вкладка для поиска
  :tabnew              Пустая вкладка для заметок

# Типичный workflow с вкладками:
Tab 1: Все книги (обзор)
Tab 2: Текущая "читаемая" библиотека (tag:reading)
Tab 3: Результаты AI-сессии
Tab 4: Активный поиск
```

---

## 8. Командная строка (:) — полный справочник

### 8.1 Базовые команды

```
── ФАЙЛОВЫЕ ОПЕРАЦИИ ──────────────────────────────────────────

:w              Сохранить текущую карточку
:w {file}       Сохранить карточку в файл
:wa             Сохранить все несохранённые карточки
:q              Quit (если нет несохранённых)
:q!             Quit без сохранения
:wq / :x        Сохранить и выйти
:qa             Quit all (закрыть все буферы)
:qa!            Quit all без сохранения

── НАВИГАЦИЯ ──────────────────────────────────────────────────

:e {path}       Edit (открыть файл книги / карточку)
:e!             Перечитать карточку с диска (отменить изменения)
:cd {path}      Изменить рабочую директорию
:pwd            Показать текущую директорию
```

### 8.2 Операции с библиотекой

```
── БИБЛИОТЕКА ─────────────────────────────────────────────────

:add {file}                 Добавить книгу из файла
:add --title "..." --author "..."  Создать карточку вручную
:del                        Удалить с подтверждением
:del!                       Удалить без подтверждения

:tag {name}                 Создать тег
:tag del {name}             Удалить тег
:tag rename {old} {new}     Переименовать тег
:tag merge {a} {b}          Объединить теги (a → b)

:lib {name}                 Создать библиотеку
:lib del {name}             Удалить библиотеку
:lib rename {old} {new}     Переименовать библиотеку

:mkdir {path}               Создать папку
:rmdir {path}               Удалить папку

:move {id} lib:{name}       Переместить книгу в библиотеку
:move {id} folder:{path}    Переместить книгу в папку
:copy {id} lib:{name}       Копировать книгу в библиотеку

:sort title                 Сортировать по заголовку
:sort author                Сортировать по автору
:sort year                  Сортировать по году
:sort rating                Сортировать по рейтингу
:sort added                 Сортировать по дате добавления
:sort!                      Обратная сортировка (с !)
```

### 8.3 Глобальные команды (:g)

```
── ГЛОБАЛЬНЫЕ КОМАНДЫ ─────────────────────────────────────────

Синтаксис: :g/{pattern}/{command}
Pattern это DSL поиска omniscope (не regexp файлов!)

:g/#rust/cs read            У всех книг с тегом #rust → статус read
:g/@author:Klabnik/ml fiction  Все книги Klabnik → в библиотеку fiction
:g/r:5/y                    Yank все пятизвёздочные в регистр
:g/y:2010/dat old           Удалить тег all и добавить тег old
:g/has:file NOT has:summary/ai index  AI-индексировать без summary
:g/status:reading/r         Список всех "в процессе чтения"

:v/{pattern}/{command}      Инвертированный глобальный
:v/has:file/del!            Удалить все карточки без файла
:v/#tag/ai suggest-tags     AI предложит теги для непомеченных

── SUBSTITUTION (замена в карточках) ──────────────────────────

:s/old/new/                 Заменить в поле текущей книги
:s/old/new/g                Заменить все вхождения в карточке
:%s/old/new/g               Заменить во ВСЕХ карточках
:g/#rust/s/Steve/S./g       В книгах с тегом rust заменить "Steve" на "S."
```

### 8.4 Диапазоны команд

```
── ДИАПАЗОНЫ ──────────────────────────────────────────────────

Синтаксис: :[range]{command}

{N}             Книга номер N
{N},{M}         С книги N по книгу M
.               Текущая книга
$               Последняя книга
%               Все книги (= 1,$)
'{a}            Книга с mark 'a'
/<pattern>/     Книга, соответствующая pattern

Примеры:
:3,7d           Удалить книги с 3-й по 7-ю
:.,$cs read     Изменить статус с текущей до конца
:%y             Yank все книги
:1,10ml fiction Переместить первые 10 в 'fiction'
:'a,'bcs unread Изменить статус от mark 'a' до mark 'b'
:/rust/,/python/d  Удалить от первой книги с 'rust' до 'python'
```

---

## 9. Макросы — автоматизация повторяющихся задач

### 9.1 Запись и воспроизведение

```
── МАКРОСЫ ────────────────────────────────────────────────────

Запись:
  q{a-z}      Начать запись макроса в регистр {a-z}
              qa = записать в регистр 'a'
  q           Остановить запись

Воспроизведение:
  @{a-z}      Выполнить макрос из регистра {a-z}
  @@          Повторить последний выполненный макрос
  [count]@{a} Выполнить макрос N раз
              10@a = выполнить макрос 'a' 10 раз

Пример: макрос для стандартизации книг
  qa              начать запись в 'a'
  cT              изменить заголовок
  [correct title]
  Tab             следующее поле (Author)
  [correct author]
  Esc             выйти из INSERT
  j               следующая книга
  q               остановить запись

  10@a            применить к следующим 10 книгам
```

### 9.2 Редактирование и хранение макросов

```
-- УПРАВЛЕНИЕ МАКРОСАМИ ────────────────────────────────────────

:let @a = 'jcTnew title<Esc>'    Задать макрос программно
:reg a                            Посмотреть содержимое макроса
:macros                           Список всех сохранённых макросов

# Сохранить макрос между сессиями (в config.toml):
[macros]
a = "jcT<title><Esc>"
b = "@ics read<Esc>"    # Добавить тег 'read' и изменить статус

# Назвать макрос:
:macro save a "normalize-card"
:macro run normalize-card
```

---

## 10. AI-операции через vim motions

### 10.1 @ как AI-оператор

```
── AI OPERATOR (@) ────────────────────────────────────────────

@           Открыть AI-панель (без аргумента)
@{motion}   AI применить к объекту motion

@i          AI index current book (индексировать текущую)
@I          AI Index all unindexed (индексировать все)
@s          AI Summary — создать/обновить summary
@t          AI Tags — предложить теги
@T          AI Tags + apply — предложить И применить
@m          AI Metadata — обогатить метаданные
@f          AI Find online — найти книгу в интернете
@d          AI Deduplicate — найти дубликаты
@a          AI Ask — задать вопрос о текущей книге
@q          AI Query — открыть AI с контекстом библиотеки

# С motion:
@iil        AI index all books in current library
@s3j        AI summary для 3 книг
@Tvit       AI tags + apply для всех книг с тегом

# С visual selection:
v → выделить книги → @i    AI индексировать выделенные
V3j → @T               AI теги + применить для 4 книг
```

### 10.2 AI в командной строке

```
:ai ask "..."              Вопрос AI-библиотекарю
:ai index                  Индексировать текущую
:ai index all              Индексировать все
:ai suggest-tags           Предложить теги для текущей
:ai enrich                 Обогатить метаданные
:ai find "{title}"         Найти книгу онлайн
:ai organize               AI предложит реорганизацию
:ai audit                  Аудит библиотеки
```

---

## 11. Специальные режимы навигации

### 11.1 Быстрые переходы (go-to shortcuts)

```
── GO-TO SHORTCUTS ────────────────────────────────────────────

gh          go home         → All Books (все книги)
gl          go last         → Последняя посещённая позиция
gb          go buffers      → Список буферов
gf          go file         → Открыть файл текущей книги
gF          go Folder       → Открыть папку файла в файловом менеджере
gn          go next         → Следующий результат поиска
gN          go Next back    → Предыдущий результат поиска
gp          go parent       → Родительская папка/библиотека
gr          go root         → Корень (All Books)

# Переходы к конкретным элементам:
g1-g9       go to tab 1-9   → Перейти к N-й вкладке
```

### 11.2 f/t/F/T — поиск в пределах экрана

Аналог vim f/t для прыжков внутри видимого списка:

```
── INLINE NAVIGATION ──────────────────────────────────────────

f{char}     Find next — перейти к следующей книге, заголовок которой
            начинается на {char}
F{char}     Find prev — найти предыдущую такую книгу
t{char}     Till — перейти к книге ДО книги с {char}
T{char}     Till back

;           Повторить последний f/F/t/T вперёд
,           Повторить последний f/F/t/T назад

Примеры:
  fR         Перейти к следующей книге на "R" (Rust, React...)
  3fR        Перейти к 3-й книге на "R"
  ;          Следующая книга на "R"
  ,          Предыдущая книга на "R"
```

### 11.3 EasyMotion-like прыжки

Вдохновлено vim-easymotion: мгновенные прыжки по видимому экрану:

```
── EASYMOTION ─────────────────────────────────────────────────

Space Space     EasyMotion — подсветить все книги
                Нажать подсвеченную букву → прыгнуть

Space j         EasyMotion вниз (только ниже курсора)
Space k         EasyMotion вверх
Space /         EasyMotion по первой букве заголовка
Space w         EasyMotion по первому слову

# Отображение:
  Текущий список:
  ▶ The Rust Programming Language      [a]
    Zero to Production                 [b]
    Programming Rust                   [c]
    Async in Action                    [d]
    ...
  Нажать [b] → прыгнуть к "Zero to Production"
```

---

## 12. Конфигурация и переопределение

### 12.1 Кастомные keybindings

```toml
# ~/.config/omniscope/keybindings.toml

[normal]
# Переопределить любую клавишу
"<C-j>" = "next-panel"
"<C-k>" = "prev-panel"
"<leader>r" = "rating-change"    # leader = Space по умолчанию
"<leader>t" = "tag-picker"
"<leader>l" = "library-picker"
"<leader>a" = "ai-panel"
"<leader>f" = "find-online"

[normal.go]
# g-команды
"gl" = "goto-last-library"
"gs" = "goto-search"
"gc" = "goto-create"

[insert]
"<C-j>" = "next-field"
"<C-k>" = "prev-field"
"<C-Space>" = "autocomplete"

[visual]
"<C-a>" = "select-all"

# Отключить клавишу:
"ZZ" = "noop"
```

### 12.2 Leader key

```toml
[settings]
leader = " "    # Space (по умолчанию)
localleader = "\\"

# Все <leader> команды:
[leader]
"<leader>b" = "buffer-list"
"<leader>f" = "telescope-find"
"<leader>g" = "telescope-grep"
"<leader>r" = "recent-books"
"<leader>a" = "ai-panel"
"<leader>s" = "sort-menu"
"<leader>t" = "tag-picker"
"<leader>l" = "library-picker"
"<leader>n" = "new-book"
"<leader>d" = "diagnostics"       # = :doctor
"<leader>?" = "help"
"<leader>/" = "search-history"
"<leader>m" = "marks"
"<leader>q" = "quickfix"
"<leader>'" = "register-peek"
```

---

## 13. Визуальная индикация режимов

### 13.1 Статус-бар

```
── NORMAL ──────────────────────────────────────────────────────────
│ 󰂺  omniscope  [programming > rust]    147 books   [N]  "a  m.  ●AI │
└───────────────────────────────────────────────────────────────────

── INSERT (редактирование карточки) ────────────────────────────────
│ 󰂺  omniscope  [EDITING: The Rust Programming Language]   [I]        │
└───────────────────────────────────────────────────────────────────

── VISUAL ──────────────────────────────────────────────────────────
│ 󰂺  omniscope  [programming > rust]   5 selected   [V]              │
└───────────────────────────────────────────────────────────────────

── COMMAND ─────────────────────────────────────────────────────────
│ :g/#rust/cs read_                                                  │
└───────────────────────────────────────────────────────────────────

── PENDING (ждём второй клавиши) ───────────────────────────────────
│ 󰂺  omniscope  [programming]    147 books   [g?]                    │
└───────────────────────────────────────────────────────────────────
```

### 13.2 Индикатор оператора в ожидании

```
При нажатии d (ожидание motion):
┌──────────────────────────────────────────────────────────────┐
│ Operator: [d] — ожидает motion или text object               │
│ Примеры: dd dG dil dat d3j                                   │
└──────────────────────────────────────────────────────────────┘

При нажатии m (move operator):
┌──────────────────────────────────────────────────────────────┐
│ Move: [m] — выберите цель: l)ibrary  f)older  t)ag           │
└──────────────────────────────────────────────────────────────┘
```

---

## 14. Справочная карточка (cheatsheet)

```
╔════════════════════════════════════════════════════════════════╗
║              omniscope — VIM MOTIONS CHEATSHEET               ║
╠════════════════════════════════════════════════════════════════╣
║  НАВИГАЦИЯ          │  ОПЕРАТОРЫ          │  РЕЖИМЫ           ║
║  j/k  вниз/вверх    │  d  delete          │  i/a/o  INSERT    ║
║  h/l  панель        │  y  yank            │  v      VISUAL    ║
║  gg/G начало/конец  │  c  change          │  V      VIS-LINE  ║
║  {/}  группы        │  m  move            │  Ctrl+v VIS-BLK   ║
║  -    вверх по иерарх│  p  put             │  :      COMMAND   ║
║  Ctrl+d/u  пол-экрана│  >/<  добавить/убрать тег│  /   SEARCH    ║
╠════════════════════════════════════════════════════════════════╣
║  TEXT OBJECTS       │  g-КОМАНДЫ          │  БУФЕРЫ/ВКЛАДКИ   ║
║  il  inner library  │  gh  home           │  gb  buffers      ║
║  al  around library │  gf  open file      │  gt  next tab     ║
║  it  inner tag      │  gp  parent         │  gT  prev tab     ║
║  at  around tag     │  gc  create         │  :bnext/:bprev    ║
║  ia  inner author   │  gI  edit in EDITOR │  :tabnew          ║
╠════════════════════════════════════════════════════════════════╣
║  БЫСТРОЕ ИЗМЕНЕНИЕ  │  MARKS              │  REGISTERS        ║
║  cT  title          │  m{a-z}  set mark   │  "{a-z}  named    ║
║  ca  author         │  '{a-z}  jump mark  │  "+  clipboard    ║
║  cy  year           │  '{A-Z}  global mark│  "_  black hole   ║
║  ct  tags           │  ''  last pos       │  "0  last yank    ║
║  cs  status         │  :marks  list all   │  :reg  list all   ║
║  cr  rating         │  Ctrl+o/i  jump list│  "ayy  yank to a  ║
╠════════════════════════════════════════════════════════════════╣
║  AI OPERATOR        │  UNDO/REDO          │  MACRO            ║
║  @i  index          │  u   undo           │  q{a}  record     ║
║  @s  summary        │  Ctrl+r redo        │  @{a}  play       ║
║  @t  suggest tags   │  U   undo all card  │  @@  repeat last  ║
║  @m  metadata       │  :earlier/:later    │  [N]@{a}  N times ║
║  @   AI panel       │  :undolist          │  :macros  list    ║
╚════════════════════════════════════════════════════════════════╝
```

---

## 15. Примеры типичных workflows

### Workflow 1: Добавить и организовать книгу
```
A               Открыть браузер файлов → выбрать PDF
                → Автоматически создаётся карточка с базовыми метаданными
i               Войти в INSERT, дополнить поля
  Tab           Перейти по полям
  [заполнить]
Esc             Сохранить
@m              AI обогатить метаданные
@i              AI индексировать (summary + TOC)
@T              AI предложить теги и применить
ml              Переместить в нужную библиотеку
```

### Workflow 2: Массовая реорганизация
```
/               Telescope: найти все книги без тегов
Ctrl+q          Отправить результаты в quickfix
:copen          Открыть quickfix список
# Для каждой книги:
cn              Следующая книга
@t              AI: предложить теги
@T              Применить предложенные
```

### Workflow 3: Перенести все книги автора в другую библиотеку
```
/*              Поиск под курсором по автору текущей книги
Ctrl+q          Все результаты в quickfix
:g/@author:{name}/ml fiction    Переместить все в 'fiction'
```

### Workflow 4: Ежедневный ревью непрочитанных
```
:tabnew tag:unread   Открыть новую вкладку с непрочитанными
gg                   Перейти к первой
j / k                Навигация
cs                   Изменить статус если начал читать
@a                   Спросить AI: "О чём эта книга?"
```

### Workflow 5: Макрос для нормализации карточек
```
# Записать макрос:
qa                  начать запись
cT                  изменить заголовок (ввести нормализованный)
[Tab][исправить]
Esc                 выйти из INSERT
@m                  AI обогатить метаданные
j                   следующая книга
q                   остановить запись

# Применить к 20 книгам:
20@a
```

---

*Vim motions — это язык. Чем лучше ты его знаешь, тем быстрее говоришь с библиотекой.*
