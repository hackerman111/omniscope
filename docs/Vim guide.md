# ⌨️ Omniscope: Руководство по Vim-управлению

Omniscope построен на мощном Vim-подобном движке, который превращает управление библиотекой в непрерывный, быстрый и клавиатурноориентированный процесс. Вместо того чтобы тянуться за мышью для выделения нескольких книг и изменения их тегов, вы можете просто набрать `c3j` (изменить эту и следующие три книги) или `dit` (удалить все книги, имеющие те же теги, что и текущая).

В этом документе описывается полная спецификация и руководство по всем реализованным командам.

---

## 🏗 Базовая Грамматика

Команды в Omniscope строятся по классической Vim-грамматике:

**`[count] [operator] [motion / text-object]`**

*   **count** (число): Сколько раз нужно повторить действие или к скольким элементам его применить. (Например, `5j` — переместиться на 5 строк вниз).
*   **operator** (глагол): Что сделать. (Например, `d` — удалить, `y` — скопировать, `c` — изменить, `>` — добавить тег).
*   **motion / text-object** (существительное): К чему применить действие. Может быть перемещением (`j`, `G`, `$`) или структурным объектом (`ib` — текущая книга, `al` — вся библиотека).

**Примеры:**
*   `dj` — Удалить текущую и следующую книгу (оператор `d` + перемещение `j`).
*   `y5k` — Скопировать (yank) текущую и 5 предыдущих книг.
*   `cib` — Изменить (`c`) текущую книгу (`ib`).
*   `>al` — Добавить тег (`>`) ко всем книгам текущей библиотеки (`al`).

---

## 🚦 Режимы Работы

Как и Vim, Omniscope работает в нескольких режимах:
*   **Normal (`Esc`)** — Режим по умолчанию. Используется для навигации и отдачи команд.
*   **Visual (`v`, `V`, `Ctrl+v`)** — Выделение элементов перед применением команды.
*   **Command (`:`)** — Режим ввода длинных команд со строки состояния (поиск, фильтрация, настройки).
*   **Search (`/`, `?`)** — Интерактивный поиск по метаданным.
*   **Pending** — Промежуточный режим, когда вы нажали оператор (например, `d`), и Omniscope ждет от вас направления (`j`, `gg`, `iw`).

---

## 🧭 Навигация и Скроллинг (Motions)

Перемещения работают как сами по себе (для прокрутки списка), так и в связке с операторами.

### Основные прыжки
*   `j` / `Down` — На одну книгу вниз.
*   `k` / `Up` — На одну книгу вверх.
*   `h` / `Left` — Сместиться влево (в панель библиотек).
*   `l` / `Right` — Сместиться вправо (в предпросмотр книги).
*   `gg` — В самое начало списка (или `[count]gg` для перехода к конкретному номеру).
*   `G` — В самый конец списка (или `[count]G` для перехода к конкретному номеру).
*   `0` — В начало (аналог `gg`).
*   `$` — В конец (аналог `G`).

### Скроллинг окна
*   `Ctrl+d` / `Ctrl+u` — Опуститься / Подняться на пол-экрана.
*   `Ctrl+f` / `Ctrl+b` — Опуститься / Подняться на целый экран (Page Down / Page Up).
*   `Ctrl+e` / `Ctrl+y` — Прокручивать само окно (viewport), **не меняя** выделенный элемент (удобно для чтения длинных списков, когда нужно подглядеть, что ниже/выше).
*   `zz` / `zt` / `zb` — Отцентрировать текущую книгу по центру (`zz`), по верхнему краю (`zt`) или по нижнему краю (`zb`) экрана.
*   `H` / `M` / `L` — Переместиться к самой **H**igh (верхней), **M**iddle (средней) или **L**ow (нижней) книге, которая сейчас видна на экране.

### Навигация по символам в названиях
*   `f<символ>` — Прыгнуть **на** следующую книгу, название которой начинается с `<символ>`.
*   `F<символ>` — Аналогично, но поиск **назад**.
*   `t<символ>` — Поиск вперед, курсор останавливается **до** найденной книги (над ней).
*   `T<символ>` — Поиск назад, курсор останавливается **после** найденной книги.
*   `;` — Повторить последний поиск символа (`f/F/t/T`) в том же направлении.
*   `,` — Повторить последний поиск символа, но в **обратном** направлении.

### EasyMotion
Прыжки для глаз — моментальный переход к объектам на экране без спама клавишами:
*   `Space Space` — Каждой книге на экране присваивается буква. Нажмите букву для перехода.
*   `Space j/k` — То же самое, но только для книг ниже (`j`) или выше (`k`) курсора.
*   `Space /` — Режим фильтрации: введите первую букву названия книги, к которой хотите перейти, и EasyMotion подсветит только подходящие.

### Иерархия и Группы
*   `-` или `Backspace` — Подняться в иерархии (из результатов поиска/фильтрации обратно к общему списку).
*   `{` / `}` — Переход к предыдущей/следующей логической группе (например, к следующему автору при сортировке по авторам).
*   `[` / `]` — Аналог `{` / `}`.
*   `gp` — Перейти к родителю (Go Parent).
*   `gr` — Сброс фильтров, показать все книги (Go Root).
*   `gh` — Домой (Go Home).

---

## 🛠 Операторы (Глаголы)

Это действия, которые требуют завершения через Motion или Text Object. Если нажать оператор **дважды** (например, `dd`, `yy`), он применится только к текущей выбранной книге.

*   `d` (Delete) — Удалить книгу.
*   `y` (Yank) — Скопировать книгу (или её метаданные) в буфер.
*   `c` (Change) — Разрушающее изменение или **быстрое редактирование**:
    *   `c<motion/object>` — Удаляет книги и отправляет статусное сообщение (в рамках TUI работает похоже на `d`).
    *   **Quick Edits** (удобно для одной книги, не требует text object):
        *   `ca` — Редактировать авторов.
        *   `ct` — Редактировать теги.
        *   `cr` — Изменить рейтинг.
        *   `cs` — Прокрутить статус чтения (Unread -> Reading -> Read -> DNF).
        *   `cy` — Редактировать год.
        *   `cn` — Добавить заметки/notes.
*   `>` (Add Tag) — Добавить новый тег к указонному диапазону. 
*   `<` (Remove Tag) — Удалить тег у указанного диапозона.

---

## 📦 Текстовые объекты (Существительные)

Применяются сразу после Оператора (например, `di[объект]`). Бывают двух видов:
*   `i` (inner) — Внутреннее содержимое. Обычно исключает сам контейнер.
*   `a` (around) — Вместе с контейнером.
 
Доступные объекты:
*   `ib` / `ab` (Book) — Только текущая книга. (например: `yib` - скопировать текущую книгу).
*   `il` / `al` (Library) — Все книги, принадлежащие к той же "Библиотеке", что и текущая книга.
*   `it` (Inner Tag) — Все книги, имеющие **абсолютно те же** теги, что и текущая.
*   `at` (Around Tag) — Все книги, пересекающиеся хотя бы по **одному** тегу с текущей.
*   `ia` / `aa` (Author) — Все книги с тем же главным автором.
*   `iy` (Year) — Все книги того же года выпуска.
*   `if` / `af` (Folder/Filter) — Все книги на экране (активный фильтр/список).

---

## 👁‍🗨 Визуальный Режим (Множественное Выделение)

Когда `[count][operator]` не подходит, вы можете выделить объекты визуально:
*   `v` — Обычный визуальный режим (выделение по одной книге).
*   `V` — Построчный визуальный режим.
*   `Ctrl+v` — Блочный визуальный режим (на будущее, пока работает как обычный).
*   `Space` (в Visual Mode) — Инвертировать выделение текущей книги (точечное выделение). Пробежаться по списку и точечно выделить нужные книги - `j <Space> j j <Space>`.
*   `o` — Развернуть направление выделения (поменять курсор и "якорь" местами). Например: выделили `j j j`, вспомнили, что надо было выделить и сверху, нажали `o`, сделали `k k k k`.
*   `Ctrl+a` — Выделить всё (`af`).
*   `Ctrl+q` — Отправить всё выделенное в Quickfix List.

После выделения просто нажмите нужный оператор (например, `d` или `y`). Выделение пропадет.
*   `gv` (из Normal Mode) — Снова выделить то, что было выделено в прошлый раз.

---

## 🌟 Маркеры, Истории Прыжков и Макросы

### Маркеры (Marks)
Для быстрого сохранения позиций (закладок):
*   `m{a-z}` — Установить локальную метку (например, `ma`).
*   `m{A-Z}` — Установить глобальную метку (доступную между разными проектами/открытыми вкладками).
*   `'{a-z}` или `` `{a-z} `` — Перепрыгнуть к метке `a`.
*   `''` — Прыжок в последнюю позицию, **до** того как вы сделали свой последний большой прыжок (сохраняет, откуда вы прыгнули).
*   `'<` и `'>` — Перепрыгнуть в начало или конец вашего последнего Визуального выделения.

### История прыжков (Jump List)
Omniscope запоминает историю дальних перемещений (`gg`, `G`, `/`, меток).
*   `Ctrl+o` — Прыгнуть **назад** в историю прыжков (Older). 
*   `Ctrl+i` — Прыгнуть **вперед** в истории прыжков (nIewer).

### Макросы (Macros)
Позволяют записать любую последовательность клавиш и быстро воспроизводить её:
*   `q{a-z}` — Начать запись макроса в регистр (например, `qa`). На экране появится индикация. Запишите нужные действия (например, прыжок, смену тега, и т.д.).
*   `q` — Остановить запись.
*   `@{a-z}` — Воспроизвести записанный макрос (например, `@a`).
*   `@@` — Воспроизвести тот же макрос, который воспроизводился последний раз (повторить).
*   `5@@` — Воспроизвести последний макрос 5 раз подряд.

---

## 🔍 Интерактивный Поиск (Search)

В Omniscope есть продвинутый "Fuzzy" внутристраничный поиск и Telescope:
*   `/` — Начать поиск вперед по текущим результатам списка.
*   `?` — Начать поиск назад.
*   `n` — Найти следующее совпадение (по направлению первоначального поиска).
*   `N` — Найти предыдущее совпадение (в обратном направлении).
*   `*` — Взять автора текущей книги и найти следующую книгу этого же автора.
*   `#` — Аналогично, но искать автора назад.

**Telescope & Палитра Команд (`Z` / `Space Space Space` / `gb`)**
Открывает оверлей с мощным поисковиком `nucleo`. Поддерживает DSL-запросы (`@author:asdf #ai`).

---

## 📋 Регистры (Буфер обмена)

Omniscope поддерживает работу с разными независимыми "карманами" (регистрами) копирования.  
Чтобы скопировать текст/книгу в конкретный регистр, перед оператором нажмите кавычку `"` и имя регистра.

*   `"a-z` — Именованные регистры. (Например, `"ayib` - скопировать книгу в регистр `a`).
*   `"0-9` — История удалений (каждое крупное удаление/книга попадает в кольцевую историю регистра).
*   `"+` — Системный буфер обмена (напрямую связано с ОС).
*   `"*` — Системный Primary-буфер (выделение мышью в Linux X11/Wayland).
*   `"_` — Регистр черной дыры `_`. Все, что удалено в него (`"_dd`), не затирает нормальный буфер обмена (безопасное удаление).

Вставка из буфера делается клавишей `p` (Paste). Она вываливает содержимое дефолтного безымянного регистра, если не указано иначе (например, `"+p`).

---

## 🚀 Командный Режим (`:`)

Используется для манипуляций библиотеками или сложных команд, которые не ложатся на клавиши.
Нажатие `:` открывает ввод в статусной строке;
Стрелки `Up`/`Down` позволяют навигироваться по ранее введенным командам (Command History).

**Справочник главных команд:**
*   `:w`, `:write` — Принудительно сохранить состояния на диск.
*   `:q`, `:quit` — Выйти из TUI.
*   `:wq` — Сохранить и выйти.
*   `:sort <field>` (:sort title, year, rating, frecency) — Изменить сортировку.
*   `:lib <name>` — Отфильтровать отображение только конкретной библиотекой.
*   `:tag <name>` — Отфильтровать по конкретному тегу.
*   `:marks` — Показать все текущие установленные метки в статусбаре.
*   `:delmarks <marks>` — Стереть метки (например, `:delmarks abc` сотрет метки a, b, c).
*   `:reg`, `:reg <char>` — Посмотреть содержимое всех регистров или одного конкретного.
*   `:macros` — Показать все записанные в данной сессии макросы.
*   `:undolist` — Показать статистику стека истории (отмен/возвратов).
*   `:earlier 5m`, `:earlier 1h` — Путешествие во времени: откатить все правки, сделанные за последние N минут/часов (Time Travel Undo).
*   `:doctor` — Просмотр внутренней служебной статистики сессии.
*   `:copen`, `:cclose` — Открыть/закрыть панель Quickfix.
*   `:cn`, `:cp` — Следующий / Предыдущий элемент в Quickfix-списке.
*   `:cdo <command>` — Запустить пакетную операцию по всем элементам из Quickfix.

**Глобальный Редактор (Regex)**:
*   `:g/pattern/d` — Найти все книги, подходящие под регулярное выражение `pattern` (по названию/авторам/тегам) и выполнить действие (`d` - удалить).
*   `:g/pattern/tag foo` — Выдать тег `foo` всем найденным и подходящим под regex книгам.

---

## 🗂 Дополнительные Клавиши (g-commands)

Команды, с префиксом `g`, для особых свойств объектов или навигации:
*   `gf` — (Go File) Открыть подключенный "реальный" файл электронной книги/статьи через стандарнтуют смотрелку ОС.
*   `gI` — Открыть метаданные `.json` карточки в редакторе, указанном в переменной `EDITOR` вашей оболочки (например, в Neovim).
*   `gv` — Заново выделить то, что находилось в вашем прошлом Визуальном Выделении.
*   `gz` — Алиас для `zz` (отцентрировать).
*   `g*` — Найти другие книги с текущим автором (alias к `*`).
*   `gB` — Откатиться на одно состояние назад (буфер, alias к `Ctrl+o`).
*   `gb` — Посмотреть список буферов/библиотек (Telescope overlay).
*   `gt` — Быстро открыть редактирование названия (Title) текущей карточки.

### Разное
*   `u` — Отмена последней операции (Undo).
*   `Ctrl+r` — Повтор того, что отменили (Redo).
*   `Enter` — Выбрать (если в боковой панели библиотек).
*   `S` — Окно быстрой сортировки (Press `S` then `y` for year desc, `t` for title, etc.).
*   `a` — Создать новую карточку (открыть диалог добавления).
