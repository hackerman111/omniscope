# Архитектура хранения и Управление библиотеками

Одной из главных архитектурных особенностей Omniscope является децентрализованный подход к хранению данных, вдохновлённый Git. Вся информация, необходимая для работы библиотеки, хранится в скрытой директории `.libr/` прямо в папке с вашими книгами.

## Децентрализованная архитектура `.libr/`

Исторически программы-каталогизаторы хранят все свои данные где-то глубоко в системе (например, `~/.local/share/APPNAME/`). Это создает проблемы:
1. Синхронизировать базу данных вместе с файлами сложно.
2. Невозможно иметь две полностью независимые библиотеки (например, "Работа" и "Хобби").
3. Перенос библиотеки на другой диск ломает пути к файлам.

**Omniscope решает это через директорию `.libr/`:**
* **Изолированность:** Все карточки, база данных полнотекстового поиска (FTS) и индексы кэша лежат в папке `.libr/`, которая живет *там же, где и сами файлы книг*.
* **Портативность:** Вы можете скопировать всю папку с книгами и папкой `.libr/` на флешку, вставить в другой компьютер, запустить `omniscope` — и всё будет работать мгновенно, без переиндексации.
* **Независимость:** У вас может быть любое количество библиотек. Omniscope автоматически "находит" библиотеку, сканируя текущую директорию и двигаясь вверх по дереву папок (по аналогии с тем, как Git ищет `.git`).

### Структура `.libr/`
При инициализации библиотеки (`omniscope init`) создается следующая структура:
```text
.libr/
├── library.toml    # Главный манифест библиотеки (имя, ID, версия)
├── cards/          # Резервные копии карточек в формате JSON (human-readable)
├── db/             # База данных SQLite (omniscope.db)
├── cache/          # Кэши обложек, эмбеддингов и API
├── undo/           # История изменений (для функции undo/redo)
└── backups/        # Автоматические бэкапы базы данных
```

---

## Управление библиотеками (CLI)

Вы можете управлять своими библиотеками через CLI (интерфейс командной строки).

### Инициализация
Чтобы превратить любую папку с книгами или статьями в библиотеку Omniscope:
```bash
cd ~/MyBooks
omniscope init . --name "Мои Книги"
```
Команда создаст структуру `.libr/` и зарегистрирует новую библиотеку в глобальном реестре (в `~/.config/omniscope/config.toml`).

### Реестр библиотек
Omniscope запоминает все библиотеки, которые вы инициализировали.
```bash
# Посмотреть список всех библиотек
omniscope libraries list
# Вывод:
#   📚 Мои Книги — /home/user/MyBooks *
#   📚 Работа — /home/user/WorkDocs

# Забыть библиотеку (информация в .libr сохраняется, удаляется только из реестра)
omniscope libraries forget /home/user/WorkDocs
```

---

## Система папок и шаблонов (Folder Management)

Omniscope поддерживает реальную структуру папок. Когда вы создаете папку в Omniscope, она создается *реально на диске*, а не только как виртуальный тег в базе данных.

### Скаффолдинг (Генерация структуры)
Чтобы не создавать папки вручную, вы можете развернуть готовую архитектуру. Встроенные шаблоны:
* `research`: Для научных статей (`papers/reading`, `papers/read`, `books/textbooks`, `notes/` и др.)
* `personal`: Для личной библиотеки (`fiction/`, `non-fiction/`, `reference/`, `to-read/` и др.)
* `technical`: Для IT-литературы (`programming/rust`, `programming/python`, `courses/` и др.)

```bash
# Посмотреть, какие папки будут созданы (без реального создания)
omniscope folder scaffold personal --dry-run
# Выведет: 📁 fiction, 📁 non-fiction, 📁 reference ...

# Применить шаблон (создаст папки на диске и зарегистрирует их в БД)
omniscope folder scaffold personal
```

### Синхронизация файловой системы (Sync)
Файловая система — первична. Вы можете создавать, перемещать и удалять директории через обычный файловый менеджер (или терминал), а затем попросить Omniscope найти расхождения:

```bash
omniscope folder sync
```
* **⊕ New on disk:** Папки, которые вы создали через `mkdir` или проводник. Они будут добавлены в библиотеку.
* **⊘ Missing on disk:** Папки, которые пропали с диска (вы их удалили).
* **？ Untracked files:** Файлы книг, которые есть на диске, но для которых еще не созданы карточки в библиотеке.

---

## Умное сканирование (File Scanning)

Вместо того чтобы вручную импортировать каждый файл, вы можете добавить (или скинуть) сразу сотню книг в папку библиотеки, а затем запустить сканер:

```bash
# Просто найти все неучтенные файлы (без создания карточек)
omniscope scan

# Найти все неучтенные файлы и автоматически создать для них карточки
omniscope scan --auto-create-cards
```

Как это работает:
1. Omniscope рекурсивно обходит (walk) всю директорию библиотеки (игнорируя скрытые папки и саму `.libr`).
2. Ищет файлы с известными расширениями (PDF, EPUB, FB2, DJVU и т.д.).
3. Сравнивает полные пути с путями, зарегистрированными в базе данных SQLite.
4. Выдает отчет (Known, New). При флаге `--auto-create-cards` извлекает базовые метаданные из названия файла (например, парсит паттерн "Автор - Название.pdf") и добавляет их в библиотеку.

### Формат хранения карточек (JSON Cards)
Все карточки дублируются в папку `.libr/cards/` в виде обычных `.json` файлов. Это делает ваши данные устойчивыми: даже если база данных SQLite повредится или исчезнет, вся библиотека может быть перестроена исключительно на основе этих JSON-файлов.

Для ручной синхронизации карточек (из файловой системы в БД) предусмотрена команда:
```bash
omniscope sync
```
Она прочитает все JSON-карточки и обновит или создаст соответствующие записи в SQLite базе.
